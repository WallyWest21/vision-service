<?xml version="1.0" encoding="utf-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:MauiClient.Views"
       x:Class="MauiClient.AppShell"
       Title="Vision Service"
       Shell.NavBarIsVisible="True">

    <!-- Dieter Rams: navigation should be self-evident and unobtrusive.
         Tabs ordered by frequency of use — the tool stays out of the way. -->

    <TabBar>

        <ShellContent Title="Live"
                      Icon="yolo.png"
                      ContentTemplate="{DataTemplate views:LiveFeedPage}"
                      Route="LiveFeedPage" />

        <ShellContent Title="Detect"
                      Icon="yolo.png"
                      ContentTemplate="{DataTemplate views:YoloPage}"
                      Route="YoloPage" />

        <ShellContent Title="Vision"
                      Icon="qwen.png"
                      ContentTemplate="{DataTemplate views:QwenVlPage}"
                      Route="QwenVlPage" />

        <ShellContent Title="Pipeline"
                      Icon="pipeline.png"
                      ContentTemplate="{DataTemplate views:PipelinePage}"
                      Route="PipelinePage" />

        <ShellContent Title="Health"
                      Icon="health.png"
                      ContentTemplate="{DataTemplate views:HealthPage}"
                      Route="HealthPage" />

        <ShellContent Title="Admin"
                      Icon="admin.png"
                      ContentTemplate="{DataTemplate views:AdminPage}"
                      Route="AdminPage" />

        <ShellContent Title="Settings"
                      Icon="settings.png"
                      ContentTemplate="{DataTemplate views:SettingsPage}"
                      Route="SettingsPage" />

    </TabBar>

</Shell>
