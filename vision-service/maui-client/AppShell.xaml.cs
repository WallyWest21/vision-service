namespace MauiClient;

/// <summary>Application shell — defines tab bar navigation.</summary>
public partial class AppShell : Shell
{
    public AppShell()
    {
        InitializeComponent();
    }
}
